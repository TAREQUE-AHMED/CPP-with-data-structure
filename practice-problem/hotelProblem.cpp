#include<iostream>
using namespace std;
int main(){
    int a, b, c, d, s;

    while (cin >> a >> b >> c >> d)
    {
        s = 0;
        if (a<b)
            
        

            s = a * c;
    
    else
            s = b * c + (a - b) * d;
    
    cout << s << endl;

    return 0;
    }
}